<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- flow://signin?code=… (web-to-app handoff), flow://invite/…, flow://join/…
                 — the scheme every native Flow client answers (ANDROID.md phase 2). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="flow" />
            </intent-filter>
            <!-- Verified App Links for the server's own /join/… and /invite/… pages.
                 The host is the server this build targets (Gradle -PflowAppLinkHost);
                 verification needs that server to publish /.well-known/assetlinks.json
                 with this build's signing fingerprint (FLOW_ANDROID_CERT_SHA256). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="${appLinkHost}" android:pathPrefix="/join/" />
                <data android:scheme="https" android:host="${appLinkHost}" android:pathPrefix="/invite/" />
            </intent-filter>
        </activity>

        <!-- Keeps a huddle alive in the background (ANDROID.md phase 4). Not
             exported: only the app's own plugin starts it (a debug-build
             overlay exports it for adb-driven checks). -->
        <service
            android:name=".HuddleService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The composer's <input type=file capture> can offer the camera (ANDROID.md phase 1). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Huddles (ANDROID.md phase 4): the WebView's getUserMedia becomes the OS
         microphone prompt only if the app declares it; the audio route and the
         background call need the rest. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
